import React from "react";
import {FooterWrapper} from "@components/Footer/style";

const Footer = () => {
    return (
        <FooterWrapper>

        </FooterWrapper>
    );
};

export default Footer;
